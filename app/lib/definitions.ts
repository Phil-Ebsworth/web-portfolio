export type Project = {
    id: string;
    title: string;
    description: string;
    content: string;
    image_url: string;
    link: string;
};

export type User = {
    id: string;
    username: string;
    password: string;
};